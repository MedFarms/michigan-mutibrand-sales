name: Scrape LeafLink Sales (All Brands)

on:
  workflow_dispatch:
    inputs:
      incremental_days:
        description: "Days to pull (0 = full backfill from LEAFLINK_FROM_DATE)"
        required: false
        default: '7'
  push:
    branches: [main]
  schedule:
    # Frequent incremental refresh so "today" / "yesterday" stay current.
    # Every 30 minutes: pulls only the last few days and MERGES into the
    # committed file (fast, rate-limit friendly). Uses the default 7-day window.
    - cron: '*/30 * * * *'
    # Weekly FULL refresh, so orders amended outside the incremental window
    # (status changes, edits on older orders) still get corrected. Sundays 09:00 UTC.
    - cron: '0 9 * * 0'

permissions:
  contents: write

concurrency:
  group: scrape-leaflink-mi
  # A newer run supersedes an older one. Each run rewrites the whole data file,
  # so a superseded run has nothing worth finishing - and without this, runs
  # triggered frequently stack up behind each other when one runs long.
  cancel-in-progress: true

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run scraper
        env:
          LEAFLINK_API_KEY: ${{ secrets.LEAFLINK_API_KEY }}
          LEAFLINK_SELLER_ID: '9105'
          LEAFLINK_BRAND: ''
          LEAFLINK_FROM_DATE: '2020-01-01'
          # Incremental mode: pull only the last N days and merge into the
          # committed file instead of rescanning 2020->today on every run. The
          # scraper still does a full backfill on the first run, or any run where
          # the committed data isn't yet complete back to LEAFLINK_FROM_DATE.
          #
          # Schedule behaviour:
          #   - The WEEKLY cron (Sundays 09:00 UTC) does a full backfill ('0') to
          #     correct any older orders that changed outside the incremental window.
          #   - The 30-MINUTE cron does an incremental 7-day merge (fast).
          #   - Manual runs use the incremental_days input (default 7).
          # Only the Sunday 09:00 tick backfills; every other scheduled tick is incremental.
          LEAFLINK_INCREMENTAL_DAYS: >-
            ${{ (github.event_name == 'schedule' && github.event.schedule == '0 9 * * 0') && '0'
                || (github.event.inputs.incremental_days || '7') }}
          # 'Combined' orders are the originals that LeafLink merged into a
          # replacement order. They still carry their full total, so counting
          # them double-counts that revenue - LeafLink's own order report
          # leaves them out. 'Draft' orders were never placed.
          LEAFLINK_EXCLUDE_STATUSES: 'Cancelled,Rejected,Combined,Draft'
          LEAFLINK_PULL_INVENTORY: '1'
        run: python scraper.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # sales_data.json.gz is binary and is fully regenerated each run, so it can
          # never be merged - our fresh copy must always win. Sync to the latest remote
          # first, then lay our new file on top and commit. Retry if the remote moves.
          ok=0
          for i in 1 2 3; do
            git fetch origin main
            git reset --mixed origin/main
            git add sales_data.json.gz
            if git diff --staged --quiet; then
              echo "No data changes to commit."; ok=1; break
            fi
            git commit -m "Auto-update sales_data.json.gz [skip ci]"
            if git push origin main; then ok=1; break; fi
            echo "Push raced with remote (attempt $i) - re-syncing..."; sleep 3
          done
          [ "$ok" = "1" ] || { echo "Could not push after retries"; exit 1; }
